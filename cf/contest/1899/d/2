#include <bits/stdc++.h>
using namespace std;
using ll = long long;
int main() {
    int t; cin >> t;
    while (t--) {
        int n; cin >> n;
        vector<ll> v(n); for (int &i : v) cin >> i;
        map<ll, ll> f;
        for (ll i : v) m[i]++;
        ll cnt = 0;
        for (ll i : v) {
            if ((i == 1) || (i == 2)) continue;
        }
    }

    return 0;
}
