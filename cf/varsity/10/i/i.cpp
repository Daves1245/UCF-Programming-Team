#include <bits/stdc++.h>
using namespace std;
int main() {
    int r, c, i, j, n; cin >> r >> c >> i >> j >> n;

    vector<vector<char>> g(r, vector<char>(c, '.'));
    int dr = {1, };
    int dc = {};
    for (int i )
    return 0;
}
