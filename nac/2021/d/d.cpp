#include <bits/stdc++.h>

using namespace std;

int bt(vector<int> &v) {
    int n = v.size();
    if (i == n) {
        return 1;
    }
    int ret = 0;
}

int main() {
    int n; k; cin >> n >> k;
    vector<int> v(n);
    for (int i = 0; i < n; i++) {
        cin >> v[i];
    }

    cout << bt(v) << endl;
    return 0;
}

