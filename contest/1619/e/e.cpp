#include <bits/stdc++.h>

using namespace std;

int main() {
    int t;
    cin >> t;
    while (t--) {
        int n;
        cin >> n;
        vector<int> v(n);
        for (int i = 0; i < n; i++) {
            cin >> v[i];
        }
        vector<int> cpy = v;
        sort(cpy.begin(), cpy.end());
        for (int i = 0; i < n; i++) {
            if (cpy[i] != )
        }
    }
    return 0;
}
