/*
 * bounds
 * clear data structures
 * epsilon
 */

#include <bits/stdc++.h>

using namespace std;

int main() {
    long long n; cin >> n;
    while (n > 1000) n /= 10;
    cout << (n == 555);
    return 0;
}

